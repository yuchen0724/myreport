# 自定义报表查询系统 — 系统架构

## 整体架构

```
┌─────────────────────────────────────────────────────────────────────┐
│                         前端层 (Vue 3)                               │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ │
│  │ 仪表盘   │ │ 模板管理 │ │ 查询编辑 │ │ NL2SQL   │ │ 销售预测 │ │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘ └──────────┘ │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ │
│  │ RCA分析  │ │ AI分析师 │ │ 收藏夹   │ │ 订阅推送 │ │ SQL审核  │ │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘ └──────────┘ │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐              │
│  │ 模型对比 │ │ 定时报表 │ │ 连接池   │ │ 移动端   │              │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘              │
└─────────────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────────┐
│                      API 网关层 (FastAPI)                            │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ │
│  │ 认证授权 │ │ 路由分发 │ │ 请求验证 │ │ 响应格式 │ │ 限流审计 │ │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘ └──────────┘ │
└─────────────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────────┐
│                       业务逻辑层 (Services)                          │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ │
│  │ 模板服务 │ │ 查询服务 │ │ 导出服务 │ │ 预测服务 │ │ NL2SQL   │ │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘ └──────────┘ │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ │
│  │ RCA服务  │ │ AI分析   │ │ 订阅服务 │ │ 审核服务 │ │ 告警服务 │ │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘ └──────────┘ │
└─────────────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────────┐
│                    数据访问层 (Repositories)                         │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ │
│  │PostgreSQL│ │  Redis   │ │  Doris   │ │   Hive   │ │ 文件系统 │ │
│  │ (主存储) │ │ (缓存)   │ │ (数仓)   │ │ (数仓)   │ │ (导出)   │ │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘ └──────────┘ │
└─────────────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────────┐
│                       异步任务层 (Celery)                            │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐              │
│  │ 导出任务 │ │ 训练任务 │ │ 订阅执行 │ │ 报表生成 │              │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘              │
└─────────────────────────────────────────────────────────────────────┘
```

## 技术栈

### 前端
- **框架**: Vue 3 (Composition API)
- **UI 组件**: Element Plus
- **状态管理**: Pinia
- **路由**: Vue Router
- **HTTP 客户端**: Axios
- **构建工具**: Vite
- **拖拽排序**: vuedraggable

### 后端
- **框架**: FastAPI
- **ORM**: SQLAlchemy + Alembic（迁移）
- **数据库**: PostgreSQL
- **缓存**: Redis
- **异步任务**: Celery
- **数据仓库**: Apache Doris / Hive
- **ML**: LightGBM（销售预测）
- **LLM**: LangChain + langchain-openai

### 基础设施
- **API 文档**: Swagger/OpenAPI
- **容器化**: Docker Compose
- **测试**: Pytest

## 核心模块

### 1. 用户认证模块
- JWT Token 认证
- 基于角色的访问控制 (RBAC)
- 用户/角色/权限三级管理

### 2. 模板管理模块
- SQL 模板创建/编辑/删除
- 模板版本控制与回滚
- 模板分享功能
- 收藏夹快速访问

### 3. 查询执行模块
- SQL 查询执行（参数化防注入）
- 查询结果缓存
- 查询历史记录
- 查询订阅定时推送

### 4. NL2SQL 模块
- 自然语言转 SQL（LangChain 集成）
- 结构化输出（Pydantic Schema 驱动）
- 提示词外部化热更新
- SQL 执行失败自动修复重试
- SQL 语句性能分析

### 5. 数据导出模块
- Excel / PDF 导出
- Celery 异步导出任务
- 导出进度实时跟踪

### 6. 销售预测模块
- LightGBM 模型训练
- 训练+预测一键完成（三阶段流水线）
- 多任务进度实时显示 + 手动停止
- 模型对比分析
- 预测结果查询与图表展示

### 7. RCA 根因分析模块
- 指标配置与异常检测
- AI 驱动的根因定位
- 维度下钻分析（商品→门店）
- AI 智能解读报告（HTML 下载）

### 8. AI 数据分析师模块
- 自然语言分析需求
- AI 自动生成 SQL 并执行
- SSE 流式输出
- 多轮上下文对话

### 9. 仪表盘模块
- 可视化组件拖拽编排
- 多布局管理
- 移动端响应式适配

### 10. 系统管理模块
- 动态菜单配置
- 代理服务器管理
- 连接池实时监控
- SQL 方言自动适配
- 告警通知
- 审计日志

## 数据流

### 查询流程
```
用户输入查询参数
    ↓
前端发送请求到后端 API
    ↓
后端验证参数和权限
    ↓
从模板库加载 SQL 模板
    ↓
替换参数生成最终 SQL
    ↓
执行 SQL 查询（Doris/Hive）
    ↓
返回查询结果
    ↓
前端展示数据（表格/图表）
```

### 销售预测流程
```
用户点击「训练并预测」
    ↓
后端创建 Celery 任务
    ↓
阶段 1: 拉取历史数据 + 特征工程 + LightGBM 训练
    ↓  (进度 0-55%)
阶段 2: 基于缓存数据直接预测（无需二次拉取）
    ↓  (进度 55-95%)
阶段 3: 保存预测结果 + 更新模型状态
    ↓  (进度 100%)
前端轮询获取进度，完成后刷新图表
```

### RCA 分析流程
```
用户配置指标参数
    ↓
后端拉取指标数据
    ↓
异常检测算法识别异常点
    ↓
AI 生成根因分析报告
    ↓
前端展示异常列表 + AI 解读
    ↓
用户可下钻到具体维度
```

## 部署架构

### 开发环境
```
┌─────────────────────────────────────────┐
│           开发机器 (WSL2)                │
│  ┌──────────┐  ┌──────────┐            │
│  │  前端    │  │  后端    │            │
│  │ :3000    │  │ :8000    │            │
│  └──────────┘  └──────────┘            │
│       │             │                  │
│       └─────────────┼──────────────────┤
│                     ▼                  │
│  ┌──────────┐  ┌──────────┐            │
│  │  Redis   │  │PostgreSQL│            │
│  │  :6379   │  │  :5432   │            │
│  └──────────┘  └──────────┘            │
│                     │                  │
│                     ▼                  │
│  ┌──────────────────────────┐          │
│  │  Celery Worker           │          │
│  │  (导出/训练/订阅任务)    │          │
│  └──────────────────────────┘          │
└─────────────────────────────────────────┘
```

### 生产环境（建议）
```
┌─────────────────────────────────────────┐
│              Nginx 反向代理              │
│              :443 (HTTPS)               │
└─────────────────────────────────────────┘
                    │
        ┌───────────┼───────────┐
        ▼           ▼           ▼
┌──────────┐ ┌──────────┐ ┌──────────┐
│  前端    │ │  后端    │ │  后端    │
│  静态文件 │ │  实例1   │ │  实例2   │
└──────────┘ └──────────┘ └──────────┘
                    │
        ┌───────────┼───────────┐
        ▼           ▼           ▼
┌──────────┐ ┌──────────┐ ┌──────────┐
│  Redis   │ │PostgreSQL│ │  Doris   │
│  集群    │ │  主从    │ │  集群    │
└──────────┘ └──────────┘ └──────────┘
```

## 安全考虑

1. **认证授权** — JWT Token + RBAC 三级权限
2. **数据安全** — SQL 注入防护（参数化查询）、数据源密码 Fernet 加密
3. **网络安全** — HTTPS、CORS、请求频率限制
4. **审计追踪** — 所有 API 调用自动记录审计日志

## 性能优化

1. **前端** — 代码分割懒加载、虚拟滚动、资源压缩
2. **后端** — 数据库连接池、Redis 缓存、异步任务处理
3. **查询** — SQL 查询优化、结果集分页、Schema 语义检索减少 token 消耗
